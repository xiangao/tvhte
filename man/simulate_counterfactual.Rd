% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{simulate_counterfactual}
\alias{simulate_counterfactual}
\title{Simulate counterfactual paths under tvhte + feedback fits}
\usage{
simulate_counterfactual(
  fit,
  fit_fb,
  t0_star,
  N_star = 500,
  Y0_star = NULL,
  X0_star = NULL,
  seed = NULL
)
}
\arguments{
\item{fit}{A \code{tvhte} object.}

\item{fit_fb}{A \code{tvhte_feedback} object.}

\item{t0_star}{Counterfactual treatment timing (scalar applied to all
simulated units, or length-\code{N_star} vector). Use \code{Inf} for never-
treated counterfactuals.}

\item{N_star}{Number of counterfactual units to simulate.}

\item{Y0_star, X0_star}{Counterfactual baseline values (length-\code{N_star}
each). Default draws standard normals.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with \code{Y} (\verb{N_star x T} matrix of counterfactual
outcomes), \code{X} (\verb{N_star x T x 1} array), and \code{lambda} (\verb{N_star x 2}
matrix of drawn latent effects).
}
\description{
Implements Algorithm 1 of Botosaru and Liu (2026): given fits of the
outcome model (\code{tvhte}) and feedback model (\code{fit_feedback}), simulate
counterfactual joint trajectories \verb{\{Y_i^\{T,*\}, X_i^\{T,*\}\}} under
alternative treatment timing or initial conditions.
}
\details{
Latent \verb{lambda_i = (alpha_i, delta_\{i0\})} is drawn from the estimated
Gaussian prior; event-time effects evolve via the AR(1) on
\verb{delta_\{ij\}}; covariates evolve via the estimated feedback process;
outcomes accumulate the direct effect (\verb{delta_\{ij\}}) plus the
indirect effect (\verb{X_it' beta}).
}
