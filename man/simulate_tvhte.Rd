% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_tvhte}
\alias{simulate_tvhte}
\title{Simulate from the Botosaru-Liu (2025) TV-HTE model}
\usage{
simulate_tvhte(
  N = 500,
  T = 6,
  t0 = 3,
  J = 3,
  rho_Y = 0.5,
  rho_delta = 0.7,
  sigma_U = 1,
  sigma_eps = 0.3,
  mu_alpha = 0,
  mu_delta0 = 1,
  sigma_alpha = 0.5,
  sigma_delta0 = 0.5,
  cor_alpha_delta = 0,
  Y0_mean = 0,
  Y0_sd = 1,
  seed = NULL
)
}
\arguments{
\item{N}{Number of units.}

\item{T}{Number of post-baseline periods (i.e. observations at
t = 1, ..., T).}

\item{t0}{Treatment period (1-indexed, in \code{1:T}).}

\item{J}{Maximum event time observed in-window.}

\item{rho_Y}{Outcome AR(1) coefficient.}

\item{rho_delta}{Event-time AR(1) coefficient.}

\item{sigma_U, sigma_eps}{Standard deviations of \verb{U_\{it\}} and \verb{eps_\{ij\}}.}

\item{mu_alpha, mu_delta0, sigma_alpha, sigma_delta0, cor_alpha_delta}{Parameters of the Gaussian prior on \verb{lambda_i = (alpha_i, delta_\{i0\})}.}

\item{Y0_mean, Y0_sd}{Distribution of the baseline outcome \verb{Y_\{i,0\}}.}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{Y} (an N x T matrix), \code{Y0} (length-N baseline
vector), \code{t0} (scalar), \code{J}, \code{lambda} (N x 2 matrix of true alpha
and delta_{i0}), and \code{delta} (N x (J+1) matrix of true event-time
effects).
}
\description{
Draws a dataset of N units observed at times 0, 1, ..., T according to
\deqn{Y_{it} = \rho_Y Y_{i,t-1} + \alpha_i + \sum_{j} D_{it}^j \delta_{ij} + U_{it}}
\deqn{\delta_{ij} = \rho_\delta \delta_{i,j-1} + \varepsilon_{ij}, \quad j \geq 1}
with \verb{(alpha_i, delta_\{i0\})} jointly Gaussian.
}
\details{
All units share a common treatment period \code{t0} and event-time window
\code{0:J} (Phase 1 scope).
}
